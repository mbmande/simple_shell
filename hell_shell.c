#include <stdio.h>

/**
 * main - it prints the most important
 *
 * Return: 0
 */

int main(void)
{
	char *print;

	print = "the most important thing in the world is to be educated\n";

	printf("these are the strings %s", print);
	return (0);

}
